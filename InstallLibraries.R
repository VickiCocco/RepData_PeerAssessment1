InstallLibraries <- 
        # install the required packages
        install.packages('dplyr')
        library(dplyr)
        install.packages('dtplyr')
        library(dtplyr)
        install.packages('ggplot2')
        library(ggplot2)

